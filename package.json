{
  "name": "pdf-server",
  "version": "1.0.0",
  "description": "Convert markdown files to pdf and send them via http.",
  "main": "index.js",
  "scripts": {
    "server": "nodemon server.js",
    "production": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evan Buss",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "markdown-pdf": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.11"
  }
}
